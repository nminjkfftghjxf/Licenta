<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="plante2._3.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:plante2._3"
    Title="plante2._3">

    <ShellContent 
        Title="Skia"
        ContentTemplate="{DataTemplate local:Skia}"
        Route="Skia"/>

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:Login}"
        Route="Login" />

    <!--am adaugat astea de mai sus-->
    
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />


</Shell>
